<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8" />
    <title>Listado de Alumnos</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 text-gray-900 p-6">
    <h1 class="text-2xl font-bold mb-4">Listado de Alumnos</h1>

    <form method="GET" action="/alumnos" class="mb-6 flex flex-wrap gap-2 items-center">
        <input type="text" name="nombre" placeholder="Buscar por nombre..." class="p-2 border rounded flex-1 min-w-[200px]" />
        <select name="sort" class="p-2 border rounded">
            <option value="">Ordenar por nombre</option>
            <option value="asc">A-Z</option>
            <option value="desc">Z-A</option>
        </select>
        <button type="submit" class="bg-green-600 text-white px-4 py-2 rounded">Buscar</button>
    </form>

    <ul class="space-y-4 mb-6">
        {{#each alumnos}}
        <li class="bg-white p-4 rounded shadow-md">
            <div class="text-gray-900 space-y-1">
                <p><strong>Nombre:</strong> {{nombre}} {{apellido}}</p>
                <p><strong>DNI:</strong> {{dni}}</p>
                <p><strong>Email:</strong> {{email}}</p>
                <p><strong>Pago:</strong> {{fechaPago}}</p>
                <p><strong>Vencimiento:</strong> {{fechaVencimiento}}</p>
            </div>
            <div class="mt-2">
                <a href="/alumnos/{{_id}}" class="inline-block bg-yellow-500 text-white px-3 py-1 rounded hover:bg-yellow-600">Ver Detalle</a>

            </div>
        </li>
        {{/each}}
    </ul>

    <div class="flex gap-4 items-center">
        {{#if hasPrevPage}}
        <a href="/alumnos?page={{prevPage}}" class="bg-gray-300 px-3 py-1 rounded">Anterior</a>
        {{/if}}

        <span>Página {{page}} de {{totalPages}}</span>

        {{#if hasNextPage}}
        <a href="/alumnos?page={{nextPage}}" class="bg-gray-300 px-3 py-1 rounded">Siguiente</a>
        {{/if}}
    </div>
</body>
</html>